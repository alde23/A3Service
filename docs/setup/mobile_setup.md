# 📱 Mobile Setup Guide

This guide covers how to get the **A3Service** mobile application running within our Nx monorepo.

## 🚀 Running the App
From the workspace root, execute the standard Nx command:
```bash
npx nx start mobile